import React from "react";
import Projects from "../components/Projects";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Projects />
    </div>
  );
};

export default page;
